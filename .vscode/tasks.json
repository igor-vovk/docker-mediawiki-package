{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Docker image",
            "type": "shell",
            "command": "docker build --pull --rm -f \"Dockerfile\" -t registry.ivovk.me/dockermediawiki:latest \".\" && docker push registry.ivovk.me/dockermediawiki",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}